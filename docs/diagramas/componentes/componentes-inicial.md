# Componentes — visão inicial

A documentação inicial apenas afirmava que o MediConnect adotava arquitetura orientada a eventos, sem limites ou contratos claros. Esta representação resume esse ponto de partida.

```mermaid
flowchart LR
    U[Usuários] --> M[MediConnect]
    M --> E[Sistemas externos]
```

Consulte `../../arquitetura/arquitetura-inicial.md`.
